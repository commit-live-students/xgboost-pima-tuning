from sklearn.model_selection import GridSearchCV
from numpy import loadtxt
from xgboost import XGBClassifier
from sklearn.model_selection import train_test_split
from sklearn.metrics import accuracy_score

def myXGBoost(X_train, X_test, y_train, param_grid, KFold=3, seed=42, **kwargs):
    pass

def n_estimator(X_train, X_test, y_train, param_grid):
    pass

def depth_child(X_train, X_test, y_train, param_grid):
    pass

def gal(X_train, X_test, y_train, param_grid):
    pass

def suco(X_train, X_test, y_train, param_grid):
    pass

def finetune(X_train, X_test, y_train, param_grid):
    pass